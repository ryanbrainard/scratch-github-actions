name: ci

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  local-on-docker:
    runs-on: [ self-hosted ]
    steps:
      - uses: actions/checkout@v2
      - run: echo "hello local in docker"
  aws-tf:
    runs-on: [ self-hosted, ubuntu, example]
    steps:
      - uses: actions/checkout@v2
      - run: echo "hello aws"
